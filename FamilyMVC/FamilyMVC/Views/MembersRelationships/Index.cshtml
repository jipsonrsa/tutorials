@model IEnumerable<FamilyMVC.Models.MembersRelationships>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Members.LastName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Relationships.Kind)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Relatives.LastName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Members.LastName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Relationships.Kind)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Relatives.LastName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MembersRelationshipsID }) |
            @Html.ActionLink("Details", "Details", new { id=item.MembersRelationshipsID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MembersRelationshipsID })
        </td>
    </tr>
}

</table>
